# Serving deps for pest-detector-final.
# Pinned to the versions that worked in eval on RunPod A40 / CUDA 12.8 / torch 2.8.
# Update cautiously — transformers 5.x introduced the model.language_model.layers
# rename that broke Unsloth's load_adapter, so pinning is deliberate.

torch==2.8.0
transformers>=5.5,<6
peft>=0.19,<1         # only needed at merge_adapter.py time; uninstall in prod
accelerate>=1.13,<2
safetensors>=0.4
pillow>=10.3
numpy>=1.26,<3

# HTTP server
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9

# Optional — install only if you want flash-attn 2 speedup on Ampere+:
# pip install flash-attn --no-build-isolation
